tablechart/pw.json
*.pyc
.DS_Store
**/staticfiles/

# Environment files (all locations)
.env
**/.env
*.env
!.env.example
!**/.env.example

# Sensitive data files
donors.json
logs/chat_history.csv

# Database backup files (bind mount — backups stored on host, not in repo)
backups/
